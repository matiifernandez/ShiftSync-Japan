const Notifications = require('expo-notifications');
console.log('Keys in Notifications:', Object.keys(Notifications));
console.log('removeNotificationSubscription type:', typeof Notifications.removeNotificationSubscription);
